var NewGraphicsController = function(){


  //public attributes and methods ----------------------------------------------

  return {

    run: function(){

      //divide in two?
    /*  this.eventDispatcher.listen("mapReady && projectsModelReady", () => {
        view.map.view.graphics.loadGraphicsLayers();
        view.map.view.graphics.loadPointGraphics(model.projects.list);
      });

      this.eventDispatcher.listen("pointGraphicsLoaded && newMenuOptionSelected", (eventData) => {
        var newOptionName = eventData["newMenuOptionSelected"];
        view.map.view.graphics.filterPointGraphics(model.projects.list, newOptionName);
      });*/

    },

  };

};
